# TODO - Luxe rectangle (cards/blocks)

## Étapes

- [x] 1. Mettre à jour `styles/style.css` : réduire les `border-radius` des éléments card/block pour un rendu “rectangle premium” (rayon ~10-14px).
- [x] 2. Appliquer le même traitement aux variantes : `.card`, `.testimonial`, `.contact-panel`, `.admin-panel`, `.admin-post-item`, et (si pertinent) `.hero-image` / `.hero-copy`.

- [x] 3. Vérifier l’absence d’effets de bord : garder `overflow: hidden` et les ombres.
- [x] 4. Contrôle visuel rapide sur `index.html`, `services.html`, `atelier.html`, `editions.html`, `blog.html` (desktop + mobile).

